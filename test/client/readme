
./client -p port -a address -l messagelen -t thread_count
参数说明如下:
address和port是客户端可以直接连接的服务器ip地址和端口
messagelen是指要发送数据的长度，最小长度是15(servant(如0x00010001)计10个字节，
最前面字段指明后续有多少数据的，用4个字节，加上最少发送一个字节的数据，10+4+1=15)
thread_count是指开多少线程，这里理解为同时开启多少个客户端访问服务器并发送数据

例：
./client -p 8002 -a 127.0.0.1 -l 50 -t 5

直接 ./client 运行，表示使用默认参数

连接成功后，在终端上直接输入任何数据，然后回车，client会发送数据给服务器端，
服务器端能转发数据到相应的服务,发送的数据暂时是写死在代码里面